
export const GetOptions = () => {

    const makeOptions = [
        // placeholder
        { label: 'Make..', id: '0'},
        { label: 'BMW', id: '1'}
    ]
    const modelOptions = [
        { label: '3 Series', id: '0'}
    ];

    return {makeOptions, modelOptions}

}

export default GetOptions